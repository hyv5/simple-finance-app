import React from "react";
import { View, Text, StyleSheet } from "react-native";

/**
 * Stub KLineView component for CI builds.
 * In local development, the real native module overrides this.
 */
export const KLineView = ({ optionList, style }) => {
  return (
    <View style={[styles.container, style]}>
      <Text style={styles.text}>KLineView: Native module not installed</Text>
      <Text style={styles.subtext}>Install react-native-kline-view locally</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#f0f0f0",
  },
  text: {
    fontSize: 14,
    color: "#666",
    textAlign: "center",
  },
  subtext: {
    fontSize: 12,
    color: "#999",
    marginTop: 4,
  },
});

export default KLineView;

